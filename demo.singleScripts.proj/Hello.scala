#!/usr/bin/env scala
// scala must be installed and available on the path
// sdkman install scala
object Hello {
    def main(args: Array[String]) = {
        println("Hello, world: " + args.toList)
    }
}

